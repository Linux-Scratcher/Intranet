# Intranet
Template for a simple Intranet.<br>
# How to<br>
Clone repository
`git clone https://github.com/Linux-Scratcher/Intranet.git`<br>
Go in the folder
`cd Intranet`<br>
Install flask
`sudo apt install flask`<br>
Run
`python3 app.py`<br>
Go to url
`http://localhost:8084/`
